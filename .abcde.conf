CDROM=/dev/sr0
EJECTCD=y
PADTRACKS=y
OUTPUTDIR=/home/jhord/media/Music/rip
OUTPUTTYPE=flac
OUTPUTFORMAT='${ARTISTFILE}/${YEAR}.0 ${ALBUMFILE}/${TRACKNUM} ${TRACKFILE}'
VAOUTPUTFORMAT='Various/${YEAR}.0 ${ALBUMFILE}/${TRACKNUM} ${ARTISTFILE} - ${TRACKFILE}'
HELLOINFO="pir@e"
CDDBMETHOD=cddb
CDDBURL="http://gnudb.gnudb.org/~cddb/cddb.cgi"

mungefilename() {
  echo "$@" | sed 's#/# - #g'
}
