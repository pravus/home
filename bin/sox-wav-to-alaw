#!/bin/bash

test -z "$*" && {
    echo "usage: $0 FILE [...]"
    exit 1
}

for ifile in "$@"; do
    ofile=$(basename "${ifile/.wav/.alaw}")
    sox -twav "$ifile" -twav -ea-law - > "$ofile" || {
        echo "$ifile: conversion failed" >&2
    }
done
