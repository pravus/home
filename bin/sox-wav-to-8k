#!/bin/bash

test -z "$*" && {
    echo "usage: $0 FILE [...]"
    exit 1
}

for ifile in "$@"; do
    ofile=$(basename "${ifile}")
    sox -twav "$ifile" -twav -c1 -r8000 -b16 - > "$ofile" || {
        echo "$ifile: conversion failed" >&2
    }
done
