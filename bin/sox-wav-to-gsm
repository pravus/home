#!/bin/bash

test -z "$*" && {
    echo "usage: $0 FILE [...]"
    exit 1
}

for ifile in "$@"; do
    ofile=$(basename "${ifile/.wav/.gsm}")
    sox -twav "$ifile" -twav -egsm - > "$ofile" || {
        echo "$ifile: conversion failed" >&2
    }
done
