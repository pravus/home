#!/bin/bash

[[ -z "$*" ]] && {
  echo "usage: $0 URL [...]" >&2
  exit 1
}

for url in $@; do
  curl -s -X POST -d "{\"url\": \"$url\"}" -o /dev/null "http://abyss.nil:4151/pub?topic=youtube-dl"
  #curl -s -X POST -d "{\"url\": \"$url\"}" -o /dev/null "http://abyss.nil:5000/"
done
