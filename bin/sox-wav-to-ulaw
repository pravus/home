#!/bin/bash

test -z "$*" && {
    echo "usage: $0 FILE [...]"
    exit 1
}

for ifile in "$@"; do
    ofile=$(basename "${ifile/.wav/.ulaw}")
    sox -twav "$ifile" -twav -eu-law - > "$ofile" || {
        echo "$ifile: conversion failed" >&2
    }
done
