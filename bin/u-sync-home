#!/bin/bash

sources=(
  .bashrc
  .profile
  .inputrc
  .screenrc
  .terminfo
  .vimrc
  .vim/colors/muon.vim
  .vim/syntax
  bin/u-profile
  tmp/
)

cd $HOME
for host in "$@"; do
  rsync -q -aR --exclude 'tmp/*' "${sources[@]}" "${host}:"
done
